<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.anyu.postservice.mapper.PostMapper">
    <!--<select id="listPostDtoAfter"  resultType="com.anyu.postservice.model.dto.PostDTO">-->
    <!--    select-->
    <!--        u.id,u.nick_name,p.id,p.title,p.content,p.type,p.create_time,p.modified_time-->
    <!--    from post p inner join user u on p.user_id = u.id-->
    <!--    where-->
    <!--</select>-->
</mapper>